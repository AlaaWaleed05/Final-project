streamlit>=1.50
pandas>=2.0
numpy>=1.26
altair>=5.0
scikit-learn==1.5.2
xgboost>=2.0
shap>=0.44
joblib>=1.3
Pillow>=10.0
opencv-python-headless>=4.8
tensorflow-cpu>=2.16   # Keras 3 is needed to open the .keras file
gdown
# IMPORTANT: match scikit-learn / xgboost / tensorflow / keras versions to the Kaggle notebook
# environment that exported the models (run `pip list | grep -E "scikit|xgboost|tensorflow|keras"` there).
